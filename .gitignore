# Original delivery folder from Sara — files are already copied into
# _source-exports/, no need to have them twice in git.
Transmodel-site download from Sara/

# Cloned reference material — each contributor runs scripts/sync_sources.py locally
_references/

# MkDocs build output — regenerated from docs/
# Anchored to repo root so we don't accidentally ignore folders like
# docs/assets/images/site/ that also happen to be called "site".
/site/

# The large source-export ZIPs (~1.5 GB total).
# The XML files ARE committed as the audit trail; the ZIPs stay local.
# If a contributor needs the images/PDFs, they can re-run scripts/extract_images.py
# against the ZIPs (which we'll share separately, e.g. via a shared Drive).
_source-exports/*.zip

# Python
__pycache__/
*.pyc
.venv/

# macOS
.DS_Store
